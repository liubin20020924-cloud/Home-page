name: Sync to Gitee

# 触发条件：main 分支有新的 push 时执行
on:
  push:
    branches:
      - main
  workflow_dispatch:  # 允许手动触发

jobs:
  sync:
    name: Sync Repository to Gitee
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0  # 获取完整的 git 历史

      - name: Configure Git
        run: |
          git config --global user.name "GitHub Actions"
          git config --global user.email "actions@github.com"

      - name: Add Gitee remote
        env:
          GITEE_REPO: ${{ secrets.GITEE_REPO }}
          GITEE_TOKEN: ${{ secrets.GITEE_TOKEN }}
        run: |
          if [ -n "$GITEE_REPO" ] && [ -n "$GITEE_TOKEN" ]; then
            echo "Adding Gitee remote..."
            # 提取 Gitee 仓库地址(去除 https:// 前缀)
            GITEE_HOST=$(echo "$GITEE_REPO" | sed 's|https://||' | sed 's|http://||')
            # 添加 Gitee 远程仓库(使用 token 进行认证)
            GITEE_REPO_WITH_TOKEN="https://${GITEE_TOKEN}@${GITEE_HOST}"
            git remote add gitee "$GITEE_REPO_WITH_TOKEN"

            # 显示远程仓库信息(隐藏 token)
            echo "GitHub remote:"
            git remote -v | grep origin
            echo "Gitee remote (with token hidden):"
            git remote -v | grep gitee | sed "s/${GITEE_TOKEN}/***TOKEN***/g"
          else
            echo "Error: GITEE_REPO or GITEE_TOKEN not configured"
            echo "Please configure these secrets in GitHub repository settings"
            exit 1
          fi

      - name: Sync to Gitee
        env:
          GITEE_REPO: ${{ secrets.GITEE_REPO }}
          GITEE_TOKEN: ${{ secrets.GITEE_TOKEN }}
        run: |
          echo "Fetching all branches..."
          git fetch --all

          echo "Syncing main branch to Gitee..."
          if git push gitee main --force; then
            echo "✓ Main branch synced successfully"
          else
            echo "✗ Failed to sync main branch"
            echo "Trying to diagnose the issue..."
            git remote -v
            exit 1
          fi

          echo "Syncing all tags to Gitee..."
          if git push gitee --tags --force 2>/dev/null || true; then
            echo "✓ Tags synced successfully (or no tags to sync)"
          else
            echo "⚠ Failed to sync tags (continuing...)"
          fi

          echo "Sync complete!"
          echo "Repository: $GITEE_REPO"
          echo "Branch: main"

          # 验证同步结果
          echo ""
          echo "Verification:"
          git log --oneline -1

      - name: Sync summary
        run: |
          echo "## 🔄 仓库同步完成" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "- **GitHub**: ${{ github.repository }}" >> $GITHUB_STEP_SUMMARY
          echo "- **Gitee**: ${{ secrets.GITEE_REPO }}" >> $GITHUB_STEP_SUMMARY
          echo "- **分支**: main" >> $GITHUB_STEP_SUMMARY
          echo "- **提交**: ${{ github.sha }}" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "✅ 代码已成功同步到 Gitee"
